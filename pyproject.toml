[project]
name = "did-you-like-this-movie"
version = "0.1.0"
description = "A simple movie rating app"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
]

[dependency-groups]
dev = [
    "ruff>=0.15.8",
    "ty>=0.0.26",
]
